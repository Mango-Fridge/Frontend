class TermsService {
  // 서버 단에 약관 관련 정보 갱신 함수
  void updateTerms(String termsType) {
    switch (termsType) {
      case 'privacy policy':
      // terms로 페이지 전환
      case 'terms':
      // 2개의 약관 동의 완료 및 API 호출
    }
  }
}
