class TermsService {
  // 서버 단에 약관 관련 정보 갱신 함수
  void updateTerms(String termsType) {
    switch (termsType) {
      case 'privacy policy':
      // api 호출
      case 'terms':
      // api 호출
    }
  }
}
